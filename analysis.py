import pandas as pd
import numpy as np

def player_count(df): #This function counts the number of unique values in a datafram column to count the number of pitchers and hitters used
  return


def main():
  return
